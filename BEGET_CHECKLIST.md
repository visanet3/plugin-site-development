# ✅ Чек-лист развёртывания на Beget

Отмечайте пункты по мере выполнения.

## 📝 Подготовка (перед заказом VPS)

- [ ] Проект собирается локально без ошибок (`npm run build`)
- [ ] База данных протестирована (все миграции применяются)
- [ ] Файл `.env` настроен корректно
- [ ] GitHub репозиторий создан (если используете Git)
- [ ] Прочитан [BEGET_QUICK_GUIDE.md](BEGET_QUICK_GUIDE.md)

---

## 🛒 Заказ услуг на Beget (5 минут)

### VPS/VDS

- [ ] Зарегистрирован аккаунт на https://beget.com
- [ ] Выбран тариф:
  - [ ] **Базовый** (300 руб/мес) - для тестирования
  - [ ] **Стандарт** (600 руб/мес) - для небольшого проекта
  - [ ] **Профи** (1200 руб/мес) - для высоконагруженного
- [ ] При создании выбрано:
  - [ ] ОС: **Ubuntu 22.04**
  - [ ] Marketplace: **Node.js** ⭐ (автоматически установит Node.js, PM2, Nginx)
  - [ ] Домен: указан свой или выбран бесплатный `*.beget.app`
- [ ] VPS создан (получено письмо с данными)
- [ ] Записаны данные доступа:
  - IP адрес: `___________________`
  - Логин: `nodejs`
  - Пароль: `___________________`

### Домен (опционально)

- [ ] Домен зарегистрирован
- [ ] DNS настроены на Beget NS-серверы
- [ ] A-запись привязана к IP VPS

---

## 🔐 Первое подключение (2 минуты)

### SSH подключение

- [ ] Установлен SSH клиент (Linux/Mac: встроенный, Windows: PuTTY или WSL)
- [ ] Успешное подключение: `ssh nodejs@ваш-ip`
- [ ] Изменён пароль (опционально): `passwd`
- [ ] Настроены SSH ключи (рекомендуется):
  ```bash
  # На локальной машине
  ssh-keygen -t rsa -b 4096
  ssh-copy-id nodejs@ваш-ip
  ```

---

## 📦 Загрузка проекта (3 минуты)

### Через Git (рекомендуем)

- [ ] Проект загружен на GitHub
- [ ] На сервере выполнено:
  ```bash
  cd ~
  git clone https://github.com/username/repo.git app
  cd app
  ```

### Через SFTP (альтернатива)

- [ ] Установлен FileZilla или WinSCP
- [ ] Подключение к серверу (SFTP, порт 22)
- [ ] Файлы загружены в `/home/nodejs/app/`

---

## 🗄️ База данных PostgreSQL (5 минут)

### Создание БД

**Вариант 1: Через панель Beget**
- [ ] Открыта панель Beget
- [ ] **MySQL/PostgreSQL** → **Добавить БД PostgreSQL**
- [ ] Создана БД с параметрами:
  - Имя БД: `___________________`
  - Пользователь: `___________________`
  - Пароль: `___________________`
- [ ] Данные сохранены в надёжном месте

**Вариант 2: Через SSH**
- [ ] Выполнено на сервере:
  ```bash
  sudo -u postgres psql
  CREATE DATABASE crypto_db;
  CREATE USER crypto_user WITH PASSWORD 'secure_password';
  GRANT ALL PRIVILEGES ON DATABASE crypto_db TO crypto_user;
  \q
  ```

### Применение миграций

- [ ] Выполнено на сервере:
  ```bash
  cd ~/app/db_migrations
  
  # Применение всех миграций
  for file in *.sql; do
    psql postgresql://user:pass@localhost:5432/db_name < "$file"
  done
  ```
- [ ] Проверено количество таблиц:
  ```bash
  psql postgresql://user:pass@localhost:5432/db_name
  \dt
  \q
  ```

---

## ⚙️ Настройка окружения (3 минуты)

### Файл .env

- [ ] Создан файл `.env` на сервере:
  ```bash
  cd ~/app
  nano .env
  ```
- [ ] Заполнены все переменные:
  ```env
  DATABASE_URL=postgresql://user:pass@localhost:5432/db_name
  PORT=3001
  NODE_ENV=production
  FRONTEND_URL=https://ваш-домен.beget.app
  VITE_API_URL=https://ваш-домен.beget.app/api
  ```
- [ ] Проверена корректность:
  ```bash
  cat .env
  ```

---

## 📦 Установка зависимостей (3 минуты)

- [ ] Выполнено:
  ```bash
  cd ~/app
  npm install
  ```
- [ ] Нет ошибок установки
- [ ] Создана сборка frontend:
  ```bash
  npm run build
  ```
- [ ] Папка `dist/` содержит файлы

---

## 🚀 Запуск приложения через PM2 (2 минуты)

### Простой запуск

- [ ] Выполнено:
  ```bash
  cd ~/app
  pm2 start server/index.js --name crypto-api
  ```
- [ ] Проверен статус:
  ```bash
  pm2 status
  ```
- [ ] Приложение в статусе `online`

### Настройка автозапуска

- [ ] Выполнено:
  ```bash
  pm2 startup
  # Выполните команду которую выведет PM2
  pm2 save
  ```

### Проверка логов

- [ ] Просмотрены логи:
  ```bash
  pm2 logs crypto-api
  ```
- [ ] Нет ошибок подключения к БД
- [ ] Backend запустился на порту 3001

---

## 🌐 Настройка Nginx (3 минуты)

### Конфигурация

- [ ] Открыт конфиг:
  ```bash
  sudo nano /etc/nginx/sites-available/nodejs.conf
  ```
- [ ] Содержимое заменено на:
  ```nginx
  server {
      listen 80;
      server_name ваш-домен.beget.app;
      
      location / {
          root /home/nodejs/app/dist;
          try_files $uri $uri/ /index.html;
      }
      
      location /api {
          proxy_pass http://localhost:3001;
          proxy_http_version 1.1;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      }
  }
  ```
- [ ] Заменён домен на актуальный

### Применение конфигурации

- [ ] Проверен синтаксис:
  ```bash
  sudo nginx -t
  ```
- [ ] Перезагружен Nginx:
  ```bash
  sudo nginx -s reload
  ```

---

## 🔒 SSL сертификат (автоматически или 2 минуты)

### Let's Encrypt

- [ ] SSL сертификат уже выпущен (при создании VPS с доменом)

**Или выпустить вручную:**
- [ ] Выполнено:
  ```bash
  sudo certbot --nginx -d ваш-домен.beget.app
  ```
- [ ] Сертификат успешно установлен
- [ ] Автопродление настроено

---

## ✅ Проверка работы (5 минут)

### Frontend

- [ ] Открыт в браузере: `https://ваш-домен.beget.app`
- [ ] Страница загружается
- [ ] Нет ошибок в консоли (F12)
- [ ] Стили применяются корректно

### Backend API

- [ ] Открыт: `https://ваш-домен.beget.app/api/health`
- [ ] Ответ: `{"status":"ok","message":"API is running"}`
- [ ] Проверены другие endpoints через curl или Postman

### База данных

- [ ] Регистрация нового пользователя работает
- [ ] Вход в аккаунт работает
- [ ] Данные сохраняются в БД

### Функционал

- [ ] Получение курсов криптовалют
- [ ] Просмотр балансов
- [ ] Обмен USDT на крипту
- [ ] Обмен крипты на USDT
- [ ] Просмотр истории транзакций
- [ ] Экспорт в Excel/CSV

---

## 🔍 Логи и мониторинг (проверьте)

### PM2 логи

- [ ] Просмотрены:
  ```bash
  pm2 logs crypto-api
  ```
- [ ] Нет критических ошибок
- [ ] API запросы логируются

### Nginx логи

- [ ] Просмотрены access логи:
  ```bash
  sudo tail -f /var/log/nginx/access.log
  ```
- [ ] Просмотрены error логи:
  ```bash
  sudo tail -f /var/log/nginx/error.log
  ```
- [ ] Нет 502/504 ошибок

### PostgreSQL логи

- [ ] Проверены (опционально):
  ```bash
  sudo tail -f /var/log/postgresql/postgresql-*.log
  ```

---

## 📊 Обновление frontend API (30 минут)

- [ ] Прочитан [UPDATE_FRONTEND_GUIDE.md](UPDATE_FRONTEND_GUIDE.md)
- [ ] Добавлен импорт `API_ENDPOINTS` в компоненты
- [ ] Заменены все URL `functions.poehali.dev` на `API_ENDPOINTS.*`
- [ ] Удалён параметр `action` из body запросов
- [ ] Проект пересобран: `npm run build`
- [ ] Билд загружен на сервер
- [ ] PM2 перезапущен: `pm2 restart crypto-api`

---

## 🔄 Автоматический деплой (опционально)

- [ ] Настроен скрипт `deploy-to-beget.sh`
- [ ] Добавлены SSH переменные в `.env`:
  ```env
  SSH_HOST=123.45.67.89
  SSH_USER=nodejs
  SSH_PORT=22
  REMOTE_PATH=~/app
  ```
- [ ] Скрипт сделан исполняемым:
  ```bash
  chmod +x deploy-to-beget.sh
  ```
- [ ] Протестирован деплой:
  ```bash
  ./deploy-to-beget.sh
  ```

---

## 💾 Резервное копирование (настройте)

### Бэкап базы данных

- [ ] Создан скрипт бэкапа:
  ```bash
  nano ~/backup-db.sh
  ```
  
  Содержимое:
  ```bash
  #!/bin/bash
  pg_dump postgresql://user:pass@localhost:5432/db > ~/backups/db_$(date +%Y%m%d_%H%M%S).sql
  ```

- [ ] Скрипт сделан исполняемым:
  ```bash
  chmod +x ~/backup-db.sh
  ```

- [ ] Настроен cron для автобэкапов:
  ```bash
  crontab -e
  # Добавить: 0 3 * * * ~/backup-db.sh
  ```

---

## 🎉 Финальная проверка

### Производительность

- [ ] Время загрузки главной страницы < 3 сек
- [ ] API отвечает < 1 сек
- [ ] Нет утечек памяти (проверить через `pm2 monit`)

### Безопасность

- [ ] HTTPS работает (зелёный замок в браузере)
- [ ] `.env` файл защищён (права 600)
- [ ] Порты 22, 80, 443 открыты, остальные закрыты
- [ ] Пароли надёжные (минимум 16 символов)

### Документация

- [ ] Сохранены все пароли в менеджере паролей
- [ ] Документированы особенности настройки
- [ ] Известны команды для управления (PM2, Nginx)

---

## 📈 Дальнейшие шаги

После успешного развёртывания:

- [ ] Настроить мониторинг (UptimeRobot, Sentry)
- [ ] Добавить аналитику (Google Analytics, Яндекс.Метрика)
- [ ] Настроить CDN для статики (опционально)
- [ ] Оптимизировать производительность
- [ ] Добавить unit тесты
- [ ] Настроить CI/CD (GitHub Actions)

---

## 💰 Расходы

- VPS Beget: **300 руб/мес**
- PostgreSQL: **Включено**
- SSL: **Бесплатно**
- Домен .beget.app: **Бесплатно**

**Итого: 300 руб/мес = ~10 руб/день**

---

## 🆘 Если что-то пошло не так

### Backend не запускается

```bash
# Проверьте логи
pm2 logs crypto-api

# Перезапустите
pm2 restart crypto-api

# Проверьте .env
cat ~/app/.env
```

### База данных недоступна

```bash
# Проверьте подключение
psql postgresql://user:pass@localhost:5432/db

# Проверьте статус PostgreSQL
sudo systemctl status postgresql
```

### 502 Bad Gateway

```bash
# Backend не запущен?
pm2 status

# Проверьте Nginx
sudo nginx -t
tail -f /var/log/nginx/error.log
```

### CORS ошибки

Проверьте `FRONTEND_URL` в `.env` - должен совпадать с доменом.

---

## 📞 Поддержка

- **Beget поддержка**: support@beget.com
- **База знаний**: https://beget.com/ru/kb
- **Telegram**: @beget_support
- **Телефон**: 8 (800) 700-06-08

---

## ✅ Итоговая проверка

Все пункты отмечены? Поздравляем! 🎉

Ваш проект успешно развёрнут на Beget:
- ✅ Frontend работает
- ✅ Backend API работает
- ✅ PostgreSQL настроен
- ✅ SSL включён
- ✅ Автозапуск настроен
- ✅ Мониторинг работает

**Ссылки:**
- Frontend: `https://ваш-домен.beget.app`
- API: `https://ваш-домен.beget.app/api`
- Health: `https://ваш-домен.beget.app/api/health`

---

**Проект полностью независим и работает на Beget! 🚀**
