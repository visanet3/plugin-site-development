# 🚀 Развёртывание на Beget.com

Полное руководство по размещению вашего проекта на хостинге Beget с Node.js и PostgreSQL.

## 📋 Что нужно

- ✅ Аккаунт на Beget.com
- ✅ VPS/VDS тариф (Node.js работает только на VPS, не на shared hosting)
- ✅ Или shared hosting с SSH доступом
- ✅ PostgreSQL база данных (можно создать в панели Beget)

## 🎯 Два варианта размещения

### Вариант 1: VPS/VDS на Beget (Рекомендуем ⭐)

**Преимущества:**
- ✅ Полный контроль над сервером
- ✅ Node.js уже предустановлен
- ✅ PM2 для управления процессами
- ✅ Nginx настроен автоматически
- ✅ SSL сертификаты автоматически

**Стоимость:** От 300 руб/мес

### Вариант 2: Shared Hosting + SSH

**Преимущества:**
- ✅ Дешевле (от 179 руб/мес)
- ✅ Подходит для небольших проектов

**Недостатки:**
- ⚠️ Ограниченные ресурсы
- ⚠️ Нужна ручная настройка Node.js

---

## 🚀 ВАРИАНТ 1: Развёртывание на VPS/VDS Beget

### Шаг 1: Заказ VPS с Node.js

1. Зайдите на https://beget.com/ru/vps
2. Выберите тариф (минимум "Базовый" за 300 руб/мес)
3. При создании сервера выберите:
   - ОС: **Ubuntu 22.04**
   - Marketplace: **Node.js** (автоматически установит Node.js + PM2 + Nginx)
   - Домен: укажите ваш домен или используйте бесплатный `*.beget.app`
4. Дождитесь создания сервера (5-10 минут)

### Шаг 2: Подключение по SSH

После создания сервера вы получите:
- IP адрес сервера
- Логин: `nodejs`
- Пароль (указан в панели)

Подключитесь:
```bash
ssh nodejs@ваш-ip-адрес
```

### Шаг 3: Загрузка проекта

#### Вариант A: Через Git (рекомендуем)

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория
git clone https://github.com/ваш-username/ваш-репозиторий.git app

# Переход в проект
cd app
```

#### Вариант B: Через FTP/SFTP

Загрузите файлы через FileZilla или WinSCP в папку `/home/nodejs/app/`

### Шаг 4: Установка зависимостей

```bash
# В папке проекта
cd ~/app

# Установка зависимостей
npm install

# Сборка frontend
npm run build
```

### Шаг 5: Создание PostgreSQL базы данных

#### Через панель Beget:

1. Войдите в панель управления Beget
2. Перейдите в раздел **MySQL/PostgreSQL**
3. Создайте новую PostgreSQL базу данных
4. Сохраните данные подключения:
   - Host: `localhost` или IP сервера
   - Port: `5432`
   - Database: имя_базы
   - Username: имя_пользователя
   - Password: пароль

#### Или через SSH:

```bash
# Установка PostgreSQL (если не установлен)
sudo apt update
sudo apt install postgresql postgresql-contrib

# Создание пользователя и базы данных
sudo -u postgres psql

CREATE DATABASE crypto_exchange;
CREATE USER myuser WITH PASSWORD 'mypassword';
GRANT ALL PRIVILEGES ON DATABASE crypto_exchange TO myuser;
\q
```

### Шаг 6: Настройка переменных окружения

Создайте файл `.env`:

```bash
cd ~/app
nano .env
```

Вставьте:
```env
# Database
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/crypto_exchange

# Server
PORT=3001
NODE_ENV=production

# Frontend URL
FRONTEND_URL=https://ваш-домен.ru
VITE_API_URL=https://ваш-домен.ru/api
```

Сохраните: `Ctrl + O`, `Enter`, `Ctrl + X`

### Шаг 7: Применение миграций БД

```bash
# Установка psql клиента (если нужно)
sudo apt install postgresql-client

# Применение миграций
cd ~/app/db_migrations

# Выполните каждый файл по порядку
psql postgresql://myuser:mypassword@localhost:5432/crypto_exchange < V0001__*.sql
psql postgresql://myuser:mypassword@localhost:5432/crypto_exchange < V0002__*.sql
# ... и так далее до последнего
psql postgresql://myuser:mypassword@localhost:5432/crypto_exchange < V0125__*.sql
```

Или одной командой (если все файлы в правильном порядке):
```bash
for file in db_migrations/*.sql; do
  psql postgresql://myuser:mypassword@localhost:5432/crypto_exchange < "$file"
done
```

### Шаг 8: Настройка PM2 (управление процессами)

PM2 уже установлен на VPS с Node.js marketplace.

Создайте конфигурацию PM2:

```bash
cd ~/app
nano ecosystem.config.js
```

Вставьте:
```javascript
module.exports = {
  apps: [
    {
      name: 'crypto-exchange-api',
      script: './server/index.js',
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '500M',
      env: {
        NODE_ENV: 'production',
        PORT: 3001
      }
    }
  ]
};
```

Сохраните и запустите:
```bash
# Запуск приложения
pm2 start ecosystem.config.js

# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs crypto-exchange-api

# Автозапуск после перезагрузки
pm2 startup
pm2 save
```

### Шаг 9: Настройка Nginx

Nginx уже настроен на VPS с Node.js. Отредактируйте конфиг:

```bash
sudo nano /etc/nginx/sites-available/nodejs.conf
```

Замените содержимое:
```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;
    
    # Frontend (static files)
    location / {
        root /home/nodejs/app/dist;
        try_files $uri $uri/ /index.html;
    }
    
    # Backend API
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Перезагрузите Nginx:
```bash
sudo nginx -t
sudo nginx -s reload
```

### Шаг 10: Настройка SSL (Let's Encrypt)

SSL сертификат уже выпущен при создании VPS. Если нужно обновить:

```bash
sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

### Шаг 11: Проверка работы

Откройте в браузере:
- Frontend: `https://ваш-домен.ru`
- API Health: `https://ваш-домен.ru/api/health`

Проверьте логи:
```bash
pm2 logs crypto-exchange-api
```

---

## 🔧 ВАРИАНТ 2: Развёртывание на Shared Hosting Beget

### Требования

- ✅ SSH доступ (включите в панели Beget)
- ✅ Тариф от 179 руб/мес

### Шаг 1: Включение SSH

1. Войдите в панель Beget
2. **Сайты** → выберите домен → **SSH-доступ** → Включить

### Шаг 2: Подключение по SSH

```bash
ssh ваш_логин@ваш_логин.beget.tech
```

### Шаг 3: Установка Node.js локально

```bash
# Переход в домашнюю директорию
cd ~

# Скачивание Node.js (проверьте актуальную версию на nodejs.org)
wget https://nodejs.org/dist/v20.11.0/node-v20.11.0-linux-x64.tar.gz

# Распаковка
tar -xzf node-v20.11.0-linux-x64.tar.gz

# Удаление архива
rm node-v20.11.0-linux-x64.tar.gz

# Добавление в PATH
echo 'export PATH=~/node-v20.11.0-linux-x64/bin:$PATH' >> ~/.bashrc

# Применение изменений
source ~/.bashrc

# Проверка установки
node -v
npm -v
```

### Шаг 4: Создание сайта в панели Beget

1. **Сайты** → **Добавить сайт**
2. Домен: `ваш-домен.ru`
3. Корневая папка: `ваш-домен.ru/public_html`

### Шаг 5: Загрузка проекта

```bash
# Переход в папку домена
cd ~/ваш-домен.ru

# Клонирование или загрузка проекта
git clone https://github.com/username/repo.git .

# Или загрузите через FTP в эту папку
```

### Шаг 6: Установка зависимостей и сборка

```bash
cd ~/ваш-домен.ru

# Установка
npm install

# Сборка frontend
npm run build

# Копирование билда в public_html
cp -r dist/* public_html/
```

### Шаг 7: Создание PostgreSQL базы данных

1. В панели Beget: **MySQL/PostgreSQL** → **Добавить БД PostgreSQL**
2. Создайте базу данных
3. Сохраните данные подключения

### Шаг 8: Настройка .env

```bash
cd ~/ваш-домен.ru
nano .env
```

```env
DATABASE_URL=postgresql://user:pass@localhost:5432/database
PORT=3001
NODE_ENV=production
FRONTEND_URL=https://ваш-домен.ru
VITE_API_URL=https://ваш-домен.ru/api
```

### Шаг 9: Применение миграций

```bash
# Если psql не установлен, используйте phpPgAdmin в панели Beget
# Или через SSH:
for file in db_migrations/*.sql; do
  psql "postgresql://user:pass@localhost:5432/database" < "$file"
done
```

### Шаг 10: Настройка .htaccess для API

Создайте `.htaccess` в папке `public_html`:

```bash
cd ~/ваш-домен.ru/public_html
nano .htaccess
```

Содержимое:
```apache
# Настройка Passenger для Node.js
PassengerEnabled on
PassengerNodejs /home/п/ваш_логин/node-v20.11.0-linux-x64/bin/node
PassengerAppRoot /home/п/ваш_логин/ваш-домен.ru
PassengerAppType node
PassengerStartupFile server/index.js

# Роутинг API запросов
RewriteEngine On
RewriteCond %{REQUEST_URI} ^/api
RewriteRule ^(.*)$ http://localhost:3001/$1 [P,L]

# Роутинг frontend
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /index.html [L]
```

**ВАЖНО:** Замените `п/ваш_логин` на первую букву вашего логина и сам логин.

### Шаг 11: Перезапуск приложения

```bash
# Создайте файл tmp/restart.txt для перезапуска Passenger
cd ~/ваш-домен.ru
mkdir -p tmp
touch tmp/restart.txt
```

### Шаг 12: Проверка

Откройте: `https://ваш-домен.ru`

---

## 🔍 Troubleshooting

### Проблема: Node.js не запускается

**Решение:**
```bash
# Проверьте статус PM2 (VPS)
pm2 status

# Перезапустите
pm2 restart crypto-exchange-api

# Проверьте логи
pm2 logs
```

### Проблема: База данных не подключается

**Решение:**
```bash
# Проверьте подключение
psql "postgresql://user:pass@localhost:5432/database"

# Если ошибка аутентификации, проверьте:
# 1. Правильность логина/пароля в .env
# 2. Доступ пользователя к БД
```

### Проблема: API возвращает 502/504

**Решение (VPS):**
```bash
# Проверьте что backend запущен
pm2 status

# Проверьте порт
netstat -tulpn | grep 3001

# Перезапустите
pm2 restart all
```

**Решение (Shared):**
```bash
# Перезапустите Passenger
touch ~/ваш-домен.ru/tmp/restart.txt

# Проверьте логи
tail -f ~/ваш-домен.ru/log/passenger.log
```

### Проблема: CORS ошибки

**Решение:**
Убедитесь что `FRONTEND_URL` в `.env` совпадает с вашим доменом:
```env
FRONTEND_URL=https://ваш-домен.ru
```

---

## 📊 Мониторинг и обслуживание

### Просмотр логов (VPS)

```bash
# Логи приложения
pm2 logs crypto-exchange-api

# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Логи PostgreSQL
sudo tail -f /var/log/postgresql/postgresql-14-main.log
```

### Обновление приложения (VPS)

```bash
cd ~/app

# Получение обновлений
git pull origin main

# Установка новых зависимостей
npm install

# Пересборка frontend
npm run build

# Перезапуск backend
pm2 restart crypto-exchange-api
```

### Бэкапы базы данных

```bash
# Создание бэкапа
pg_dump postgresql://user:pass@localhost:5432/database > backup_$(date +%Y%m%d).sql

# Восстановление
psql postgresql://user:pass@localhost:5432/database < backup_20260114.sql
```

---

## 💰 Стоимость

### VPS/VDS (Рекомендуем)
- **Базовый**: 300 руб/мес (1 CPU, 1 GB RAM, 10 GB SSD)
- **Стандарт**: 600 руб/мес (2 CPU, 2 GB RAM, 20 GB SSD)
- **Профи**: 1200 руб/мес (4 CPU, 4 GB RAM, 40 GB SSD)

### Shared Hosting
- **Старт**: 179 руб/мес (3 GB)
- **Хит**: 299 руб/мес (10 GB)
- **Бизнес**: 699 руб/мес (25 GB)

### База данных PostgreSQL
- **Включено** в стоимость VPS
- **От 50 руб/мес** на Shared Hosting

---

## ✅ Чек-лист развёртывания

- [ ] Заказан VPS/VDS или Shared Hosting на Beget
- [ ] Подключен SSH доступ
- [ ] Установлен Node.js (если Shared)
- [ ] Создана PostgreSQL база данных
- [ ] Загружен проект на сервер
- [ ] Установлены зависимости (`npm install`)
- [ ] Собран frontend (`npm run build`)
- [ ] Создан файл `.env` с правильными данными
- [ ] Применены все миграции БД
- [ ] Настроен PM2 (VPS) или Passenger (Shared)
- [ ] Настроен Nginx
- [ ] Установлен SSL сертификат
- [ ] Проверена работа frontend и API
- [ ] Настроен автозапуск после перезагрузки

---

## 🎉 Готово!

Ваш проект теперь работает на Beget!

- 🌐 Frontend: `https://ваш-домен.ru`
- 🔌 API: `https://ваш-домен.ru/api`
- ✅ Health Check: `https://ваш-домен.ru/api/health`

**Поддержка Beget:** https://beget.com/ru/kb  
**Техподдержка:** support@beget.com
